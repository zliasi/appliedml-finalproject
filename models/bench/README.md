# bench

Benchmark per backend: checkpoint load time, single-slab inference latency, and test MAE. Load time is important, as the goal is a pre-process for GPAW calculations of single slabs. Each backend is thus scored based on loading time and inference latency, besides its accuracy.
